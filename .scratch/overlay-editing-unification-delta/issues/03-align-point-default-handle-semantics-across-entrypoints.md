# 03 — 对齐点位默认句柄语义到所有入口

**What to build:** 让点位编辑在所有入口上都表现为同一种 0.x 默认移动点体验。默认句柄语义、外观和回调时机保持一致，不再出现某些入口是绿色移动点、某些入口是蓝色顶点点的分叉。

**Blocked by:** 01 — 统一标绘编辑到单一 Overlay 编辑内核; 02 — 收口 Overlay 编辑句柄配置契约

**Status:** ready-for-agent

- [ ] 点位在 `OverlayService`、`MarkService` 和兼容入口上的默认句柄语义一致，并对齐 0.x 的移动点体验。
- [ ] 点位拖拽在所有入口上都实时触发 `change`，结束编辑时都返回最终快照。
- [ ] playground 和公开文档中的默认点位编辑体验不再依赖入口差异。
