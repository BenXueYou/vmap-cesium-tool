# 07 — 保留折线旋转与缩放为可配置能力

**What to build:** 让折线的旋转和缩放能力继续存在于统一编辑内核中，默认不显示，但显式开启后可以正常工作且不破坏新的默认编辑体验。

**Blocked by:** 06 — 恢复折线已落点插入与删点编辑

**Status:** ready-for-agent

- [ ] `rotate` 与 `scale` 继续属于 `OverlayEditOptions` 的公开能力。
- [ ] 折线默认编辑体验不显示旋转和缩放句柄。
- [ ] 当业务显式开启旋转或缩放时，折线在所有入口上都能使用统一编辑引擎完成对应操作。

