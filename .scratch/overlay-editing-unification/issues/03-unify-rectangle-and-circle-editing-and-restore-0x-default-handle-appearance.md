# 03 — 统一矩形与圆形编辑并恢复 0.x 默认句柄外观

**What to build:** 让矩形和圆形覆盖物切到统一编辑引擎，同时保留矩形角点编辑、圆形中心点与半径点编辑的图形语义，并把默认句柄外观对齐到 0.x。

**Blocked by:** 01 — 统一编辑入口与公开编辑契约

**Status:** ready-for-agent

- [ ] `rectangle` 在所有入口上保持角点编辑语义，并使用统一编辑会话和回调模型。
- [ ] `circle` 在所有入口上保持中心点/半径点编辑语义，并使用统一编辑会话和回调模型。
- [ ] `rectangle` 与 `circle` 的默认句柄外观对齐 0.x。

