# 06 — 恢复折线已落点插入与删点编辑

**What to build:** 让折线编辑恢复 0.x 的顶点点/边中点插点和右键删点体验，同时明确保留折线自交与回折的合法性。

**Blocked by:** 01 — 统一编辑入口与公开编辑契约

**Status:** ready-for-agent

- [ ] `polyline` 默认显示蓝色顶点点和粉色边中点，视觉语义对齐 0.x。
- [ ] 折线支持边中点立即插点并直接拖拽，且按下松开也算插点成功。
- [ ] 折线支持右键删除蓝色顶点并最少保留 2 个点，同时允许自交和回折。

