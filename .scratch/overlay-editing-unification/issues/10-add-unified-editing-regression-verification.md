# 10 — 补统一编辑回归验证

**What to build:** 用高层行为验证固定统一编辑结果，确保不同入口、不同图形和不同编辑动作不会再次分叉。

**Blocked by:** 02 — 统一点位编辑并恢复 0.x 默认句柄语义; 03 — 统一矩形与圆形编辑并恢复 0.x 默认句柄外观; 05 — 恢复多边形删点与合法性约束; 07 — 保留折线旋转与缩放为可配置能力; 08 — 收口编辑会话生命周期与选择联动

**Status:** ready-for-agent

- [ ] 回归验证覆盖 `point / rectangle / circle / polygon / polyline` 的关键编辑结果，而不是内部实现细节。
- [ ] 回归验证覆盖插点、删点、实时 `change`、最终 `end`、自相交拦截和选择联动等关键用户可见行为。
- [ ] 回归验证能证明 `OverlayService`、`MarkService` 和兼容入口在编辑结果上保持一致。
