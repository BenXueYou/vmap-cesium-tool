# 09 — 对齐统一编辑文档与 ADR

**What to build:** 让公开文档和架构决策记录完整描述新的统一编辑模型，确保用户和维护者读到的行为、默认句柄和职责边界与最终实现一致。

**Blocked by:** 02 — 统一点位编辑并恢复 0.x 默认句柄语义; 03 — 统一矩形与圆形编辑并恢复 0.x 默认句柄外观; 05 — 恢复多边形删点与合法性约束; 07 — 保留折线旋转与缩放为可配置能力; 08 — 收口编辑会话生命周期与选择联动

**Status:** ready-for-agent

- [ ] API 文档、编辑指南和兼容入口说明与最终默认交互和回调语义一致。
- [ ] 文档明确 `OverlayService` 是唯一编辑内核，`MarkService` 保留标绘编排职责。
- [ ] 新增 ADR 记录为何保留 `MarkService` 但移除其重复编辑实现。

