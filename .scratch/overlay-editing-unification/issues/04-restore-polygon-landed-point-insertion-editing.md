# 04 — 恢复多边形已落点插入编辑

**What to build:** 让多边形编辑恢复 0.x 的插点体验：默认显示蓝色顶点点和粉色边中点，按下粉色中点立即插入新的已落点并直接拖拽，按下松开也算插点成功。

**Blocked by:** 01 — 统一编辑入口与公开编辑契约

**Status:** ready-for-agent

- [ ] `polygon` 默认显示顶点点和边中点，且视觉语义对齐 0.x。
- [ ] 按下粉色边中点时立即插入新的已落点，并切换为拖拽该新顶点。
- [ ] 插点动作在所有入口上都实时触发 `change`，哪怕用户只是按下后直接松开。

