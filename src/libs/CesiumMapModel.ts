// 存根类型定义文件
// 为了保持向后兼容性，实际类型定义已移至 core/types.ts

export type {
  ToolbarConfig,
  ButtonConfig,
  CustomButtonConfig,
  SearchCallback,
  SearchResult,
  MeasurementCallback,
  ZoomCallback,
  MapType
} from '../core/types';